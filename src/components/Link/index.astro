---
// import { Link } from "gatsby";
// import { useGoatCounter } from "gatsby-plugin-goatcounter";
//

import "./link.scss";

type Props = {
  href: string;
  className?: string;
  active?: boolean;
  /**
   * passed by rehype when styling anchors
   */
  isHeading?: true;
};

const { href, className = "body-link", active, isHeading } = Astro.props;

// onClick={() => {
//     // log the event manually, but don't preventDefault()
//     count({
//       path: `ext-${href}`,
//       event: true,
//     });
//   }}
---

<a
  class:list={[isHeading ? "markdown-header" : className, active && "active"]}
  href={href}
  {...href.startsWith("/") || href.startsWith("#")
    ? {}
    : {
        target: "_blank",
      }}><slot /></a
>
